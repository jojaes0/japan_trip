# 일본 또갈지도

가보고 좋아서 또 갈지도 모르는 일본의 음식점 · 쇼핑 · 숙소 · 관광지를 나만의 팁과 함께 모아두는 페이지.
서버·빌드·API 키 없이 동작하는 정적 사이트라 GitHub Pages에서 무료로 운영됩니다.

## 기능

- **내 주변** — 기기 위치를 기준으로 저장한 장소를 가까운 순으로 정렬 (도보 10분 / 3km / 그 외). 위치는 브라우저 안에서 거리 계산에만 쓰이고 어디에도 전송되지 않습니다.
- **지역 필터** — 도시 › 상세 지역 (오사카 › 난바·도톤보리), 종류(음식점/카페/쇼핑/숙소/관광지/투어), 검색
- **지도 보기** — 한국어 지명 지도(OpenFreeMap, 무료·키 불필요)에 종류별 색상 핀 + 이름 라벨, 내 위치 표시. 범례를 누르면 그 종류만 남습니다
- **내 여행** — 장소 상세의 "내 여행에 담기"로 모아 두고(이 기기의 브라우저에만 저장), 일차 · 시간 · 메모 입력, 순서 변경, 선택/전체 삭제, 일차별 동선 지도(일차 칩을 누르면 그 일차만, 다시 누르면 전체), 공유 링크. 공유 링크를 받은 사람은 "내 여행으로 복사"로 그대로 가져갈 수 있음
- **내 여행에 직접 추가** — 내 여행에서 구글맵 링크를 붙여넣으면 이름 · 종류 · 위치를 자동으로 채워 담고(수정 가능), 공유 링크에도 함께 담김. 휴대폰 공유 링크(maps.app.goo.gl)를 자동으로 읽으려면 [tools/resolver.gs](tools/resolver.gs)를 Apps Script 웹 앱으로 배포해 그 URL을 assets/app.js 의 `RESOLVER` 에 넣어야 함 (파일 안에 설치 순서)
- **공유** — 필터 상태와 열려 있는 장소가 주소에 그대로 담깁니다. 예) `…/#city=교토`, `…/#p=<장소 id>` (상세 화면의 공유 버튼이 만들어 줍니다)

## 장소 관리

**원본은 구글맵 저장 리스트입니다.** 평소처럼 구글맵에 저장하고 코멘트를 쓰면, GitHub Actions가 30분마다 사이트로 옮겨옵니다 (GitHub 사정에 따라 몇 시간 늦어질 수 있음).

| 하고 싶은 일 | 방법 |
|---|---|
| 장소 추가 · 삭제 · 팁 수정 | 구글맵 리스트에서 |
| 이름 바꾸기 (구글맵 이름이 일본어일 때) | 구글맵 코멘트 **맨 앞**에 `[요로시 화장당]` — 사이트에는 그 이름으로 표시 (코멘트 중간의 대괄호는 그냥 글) |
| 분류 지정 | 구글맵 코멘트에 `#음식점 #스시 #야식 @우메다` (`#`종류·태그, `@`상세 지역, 띄어쓰기는 `_`) — 해시태그는 사이트의 팁에서는 자동으로 빠집니다 |
| 지금 바로 반영 | 저장소 Actions 탭 → "구글맵 동기화" → Run workflow (자동 반영은 30분~몇 시간) |
| **추천 표시** | 코멘트에 `#추천` — 목록 맨 위·배지, 지도에서 별 핀과 강조 라벨, `★ 추천` 필터 |
| 다른 리스트 추가 (예: 도쿄 맛집) | [data/lists.json](data/lists.json) 에 `{ "url": "공유 링크", "defaultType": "food" }` |
| **투어** 추가 · 수정 | [투어 구글 시트](https://docs.google.com/spreadsheets/d/1pVqDnp9qBkJyPdSGsv5_70IUaRjHG4WiqB4o55YrxkE/edit)의 `투어` 탭에 한 줄 추가 (탭 이름으로 읽으므로 순서는 무관) — 열: `주소 · 투어명 · 투어 정보 · 이동수단` (선택: `코스 · 도시 · 태그 · 팁 · 추천`). 도시는 투어 정보에서 자동 판별 |
| **티켓** (기차표 · 입장권 등) | 같은 시트의 `티켓` 탭에 한 줄 추가 — 열: `주소 · 티켓명 · 종류 · 참고사항 · 도시` (티켓명만 필수). 종류는 필터 칩, 도시를 비우면 자동 판별(없으면 "공통") |
| 투어 **코스**를 지도에 | 시트의 `코스` 칸에 방문 순서대로 구글맵 공유 링크를 콤마로 구분 — `링크, 링크, 링크` (이름을 직접 붙이려면 `[이름]링크`, 출발지는 `[출발]링크`, 도착지는 `[도착]링크` `[도착1]링크` `[도착2]링크`, 간 김에 들를 곳은 `[식사 와규덮밥]링크` `[간식 당고]링크` `[카페 …]` `[쇼핑 상점가]` `[추천 …]` — 번호·점선에서 빠지고 그 단어의 핀으로 표시, 뒤의 말은 메모). 상세 화면의 "코스 지도로 보기"나 종류=투어일 때 번호 핀과 점선으로 그려짐 |

**해시태그 예시** — 숙소: `#숙소 #온천 #가성비 @난바`

| 종류 | 해시태그 |
|---|---|
| 음식점 | `#음식점` `#맛집` `#식당` |
| 카페·디저트 | `#카페` `#디저트` |
| 쇼핑 | `#쇼핑` |
| 숙소 | `#숙소` `#호텔` `#료칸` |
| 관광지 | `#관광지` `#관광` |
| 투어 | `#투어` |

그 밖의 `#단어`는 자유 태그가 됩니다. `@난바`처럼 지역 이름의 일부만 적어도 기존 지역(`난바·도톤보리`)으로 맞춰집니다.

종류 값: `food` 음식점 · `cafe` 카페·디저트 · `shop` 쇼핑 · `stay` 숙소 · `sight` 관광지 · `tour` 투어

**분류 방식**: 코멘트에 해시태그가 있으면 그대로, 없으면 자동 추론합니다 — 종류는 리스트 기본값 + 이름·코멘트 키워드, 지역은 ① 근처(700m)에 `@지역`을 단 장소 ② [data/areas.json](data/areas.json) 지역 사전(주소 키워드 → 중심 좌표 반경) 순서.
추론이 틀렸다면 **구글맵 코멘트에 해시태그를 적어 보정**합니다. 새 장소가 추론으로 분류되면 **"새 장소 분류 확인" 이슈**가 생겨 메일로 알려줍니다.

> [data/places.js](data/places.js) 는 자동 생성 파일이라 직접 고치면 다음 동기화 때 덮어써집니다.
> 동기화는 구글의 비공식 주소를 읽습니다. 막히면 동기화만 실패하고 사이트는 마지막 데이터로 계속 동작합니다. PC에서 `node scripts/sync.mjs` 로 직접 돌릴 수도 있습니다.

## 배포 (GitHub Pages)

1. GitHub에 새 저장소를 만들고 이 폴더를 그대로 push
2. 저장소 **Settings → Actions → General → Workflow permissions** 를 *Read and write* 로 (동기화 커밋용)
3. 저장소 **Settings → Pages → Source: Deploy from a branch**, Branch `main` / `/ (root)` 선택
4. 잠시 후 `https://<아이디>.github.io/<저장소명>/` 에서 열립니다

> "내 주변"은 HTTPS에서만 동작합니다. GitHub Pages는 기본이 HTTPS라 그대로 됩니다.

## 구조

```
index.html        페이지
assets/style.css  디자인
assets/app.js     필터 · 내 주변 · 지도 · 공유
data/places.js    장소 데이터 (자동 생성)
data/lists.json   동기화할 구글맵 리스트(url) · 투어 시트(sheet)
data/stops-cache.json  투어 코스 좌표 캐시 (자동 생성)
data/areas.json   지역 사전 (동네별 주소 키워드 · 중심 좌표)
scripts/sync.mjs  동기화 스크립트
.github/workflows/sync.yml  30분마다 자동 동기화
```
